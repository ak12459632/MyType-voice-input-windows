從 GitHub 將最新版本同步拉取到 `d:\ClaudeCode\MyType Voice Input`。

執行步驟：
1. 切換到專案目錄 `d:\ClaudeCode\MyType Voice Input`
2. 執行 `git status` 確認本地是否有未提交的變更
3. 若有未提交的變更，先提示使用者決定是否要先 commit 或 stash，再繼續
4. 執行 `git pull` 從 GitHub 拉取最新版本
5. 回報拉取結果（更新了哪些檔案，或已是最新版本）
